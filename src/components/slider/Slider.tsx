import styled from 'styled-components';

export const Slider = () => {
    return (
        <StyledSlider>
             <Slide>
                <Text> About education in bsuir</Text>
                <Name> BSUIR</Name>
              </Slide>
        </StyledSlider>
    );
};

const StyledSlider = styled.div`

`
const Name = styled.span`

`
const Text = styled.p`

`
const Slide = styled.div`

`